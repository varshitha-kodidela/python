print("Hello, World!")
a = 10
b = 20

sum = a + b
print("Sum:", sum)
num = int(input("Enter a number: "))

if num % 2 == 0:
    print("Even")
else:
    print("Odd")
    a = 15
b = 9
c = 27

largest = max(a, b, c)
print("Largest number is:", largest)
num = int(input("Enter a number: "))

for i in range(1, 11):
    print(num, "x", i, "=", num * i)
    num = int(input("Enter a number: "))
fact = 1

for i in range(1, num + 1):
    fact *= i

print("Factorial:", fact)

text = input("Enter a string: ")

print("Reversed:", text[::-1])
a = float(input("Enter first number: "))
b = float(input("Enter second number: "))

print("Addition:", a + b)
print("Subtraction:", a - b)
print("Multiplication:", a * b)
print("Division:", a / b)
n = 10
a, b = 0, 1

for i in range(n):
    print(a, end=" ")
    a, b = b, a + b
    secret = 7

guess = int(input("Guess the number: "))

if guess == secret:
    print("Correct!")
else:
    print("Wrong guess!")
    import random
import string

length = 12

characters = string.ascii_letters + string.digits + string.punctuation

password = "".join(random.choice(characters) for i in range(length))

print("Generated Password:", password)
import time

sentence = "Python is fun to learn"

print("Type this sentence:")
print(sentence)

start = time.time()

typed = input()

end = time.time()

speed = len(typed.split()) / ((end - start) / 60)

print("Typing speed:", round(speed, 2), "WPM")
mood = input("How are you feeling? ").lower()

quotes = {
    "happy": "Keep shining!",
    "sad": "Better days are coming.",
    "tired": "Rest and recharge.",
    "angry": "Take a deep breath."
}

print(quotes.get(mood, "You are amazing!"))
import random
import time
import os

chars = "01"

while True:
    line = "".join(random.choice(chars) for _ in range(60))
    print(line)
    time.sleep(0.1)
    import random

while True:
    input("Press Enter to roll the dice...")
    print("You rolled:", random.randint(1, 6))

    again = input("Roll again? (y/n): ")
    if again.lower() != "y":
        break
        import time

seconds = int(input("Enter seconds: "))

while seconds:
    mins, secs = divmod(seconds, 60)
    timer = f"{mins:02d}:{secs:02d}"
    print(timer, end="\r")
    time.sleep(1)
    seconds -= 1

print("Time's up!")
import random

emojis = ["🔥", "✨", "😎", "🚀", "💀", "🌸", "🎧"]

print("Your emoji:", random.choice(emojis))
rows = 5

for i in range(1, rows + 1):
    print(" " * (rows - i), end="")
    print(str(i) * (2 * i - 1))
    message = input("Enter message: ")

encoded = ""

for char in message:
    encoded += chr(ord(char) + 2)

print("Encoded:", encoded)
import random

fortunes = [
    "You will become rich.",
    "Today is your lucky day.",
    "A surprise is waiting for you.",
    "Sleep more. You need it.",
    "Your code will finally work."
]

print(random.choice(fortunes))
import random

name = input("Enter your name: ")

symbols = ["_", ".", "X", "99"]

username = name + random.choice(symbols) + str(random.randint(10, 999))

print("Generated Username:", username)
binary = input("Enter binary number: ")

decimal = int(binary, 2)

print("Decimal:", decimal)
import time

alarm = input("Set alarm time (HH:MM:SS): ")

while True:
    current = time.strftime("%H:%M:%S")

    if current == alarm:
        print("Wake up!")
        break
        name = input("Enter a name: ")
place = input("Enter a place: ")
object = input("Enter an object: ")

story = f"{name} went to {place} carrying a {object}."

print(story)
import random

color = "#{:06x}".format(random.randint(0, 0xFFFFFF))

print("Random Color:", color)
word = input("Enter a word: ")

if word == word[::-1]:
    print("Palindrome")
else:
    print("Not Palindrome")
    while True:
    msg = input("You: ").lower()

    if msg == "hello":
        print("Bot: Hi!")
    elif msg == "bye":
        print("Bot: Goodbye!")
        break
    else:
        print("Bot: I don't understand.")
        import random

jokes = [
    "Why do programmers hate nature? Too many bugs.",

    "Python programmers don't byte, they hiss.",
    "Debugging: Removing the needles from the haystack."
]

print(random.choice(jokes))
rows = 5

for i in range(rows):
    print("*" * (i + 1))
    from gtts import gTTS

text = input("Enter text to convert into speech: ")

tts = gTTS(text=text, lang='en')

tts.save("voice.mp3")

print("Audio saved as voice.mp3")
import random

captions = [
    "When the code works on first try 😎",
    "Me fixing one bug and creating ten more 💀",
    "POV: Deadline is tomorrow",
    "Programmer by day, debugger by night"
]

print(random.choice(captions))
import speedtest

wifi = speedtest.Speedtest()

print("Downloading...")
download = wifi.download() / 1_000_000

print("Uploading...")
upload = wifi.upload() / 1_000_000

print(f"Download Speed: {download:.2f} Mbps")
print(f"Upload Speed: {upload:.2f} Mbps")
import random
import string

site = input("Website name: ")

password = ''.join(random.choice(
    string.ascii_letters + string.digits
) for _ in range(10))

with open("passwords.txt", "a") as file:
    file.write(f"{site}: {password}\n")

print("Password saved!")
import time

animation = ["|", "/", "-", "\\"]

for i in range(20):
    print(animation[i % 4], end="\r")
    time.sleep(0.2)

print("Done!")
import random

songs = [
    "Blinding Lights",
    "Perfect",
    "Levitating",
    "Shape of You"
]

print("Now Playing:", random.choice(songs))
file = open("sample.txt", "r")

text = file.read()

words = len(text.split())

print("Word Count:", words)
usd = float(input("Enter USD amount: "))

inr = usd * 83

print("INR:", inr)
from plyer import notification

notification.notify(
    title="Reminder",
    message="Drink water!",
    timeout=5
)
name1 = input("First name: ")
name2 = input("Second name: ")

score = (len(name1) + len(name2)) * 7

print("Love Percentage:", score % 101, "%")
from pyzbar.pyzbar import decode
from PIL import Image

img = Image.open("qr.png")

result = decode(img)

print(result[0].data.decode())
text = input("Enter mood: ")

emojis = {
    "happy": "😄",
    "sad": "😢",
    "cool": "😎",
    "love": "❤️"
}

print(emojis.get(text, "✨"))
import random

for i in range(5):
    color = "#{:06x}".format(random.randint(0, 0xFFFFFF))
    print(color)
    weight = float(input("Enter weight (kg): "))
height = float(input("Enter height (m): "))

bmi = weight / (height ** 2)

print("BMI:", round(bmi, 2))
import os

files = os.listdir()

for index, file in enumerate(files):
    os.rename(file, f"file_{index}.txt")

print("Files renamed!")
import sounddevice as sd
from scipy.io.wavfile import write

fs = 44100
seconds = 5

print("Recording...")

recording = sd.rec(int(seconds * fs),
                   samplerate=fs,
                   channels=2)

sd.wait()

write("output.wav", fs, recording)

print("Saved!")
import random
import string

password = ''.join(random.choice(
    string.ascii_letters + string.digits
) for _ in range(12))

print("WiFi Password:", password)
import time

input("Press Enter to start")
start = time.time()

input("Press Enter to stop")
end = time.time()

print("Elapsed Time:", round(end - start, 2), "seconds")
import random

facts = [
    "Octopuses have three hearts.",
    "Bananas are berries.",
    "Honey never spoils."
]

print(random.choice(facts))